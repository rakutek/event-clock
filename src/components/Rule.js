import React  from 'react';

const Rule = ( ) => (
    <hr
        style={{
            color: "white",
            backgroundColor: "white",
            width: 800,
        }}
    />
);

export default Rule;
